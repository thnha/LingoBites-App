## Summary

Resolved Multica issue VIB-136 (CONFLICT-01): synced `07-phase1-prd-draft.md`'s business rule `BR-REVIEW-002` — which still described the rejected SM-2 model ("Again → relearning") — with the fixed-interval, 2-outcome (`remembered`/`forgot`) + skip decision made at Design Human Gate 1 (DD-04, VIB-126). Also swept the rest of the PRD for other SM-2-specific language (`ease_factor`, stale "open decision" pointers) and fixed or explicitly justified each remaining mention. Posted the diff and rationale as a comment on VIB-136 and moved it to `in_review`.

## Changed files

- `attachments/07-phase1-prd-draft.md` — edited:
  - §5 `P1-F-03` and §7 `FR-SRS-002`: updated "xem §9.2 (open decision)" → "xem §9.2 (đã chốt, DD-04)" so they no longer contradict the now-resolved §9.2.
  - §8 `BR-REVIEW-002`: replaced `Again` → relearning wording with the approved `remembered`/`forgot` fixed-interval model.
  - §9.2 "Q1 — Thuật toán SRS": added a **RESOLVED (DECISION)** note citing DD-04/VIB-126 (Option 3 — Fixed interval chosen); kept the original 3-option list (including Option 1's SM-2 terms) as an explicitly-justified historical record rather than deleting it.
  - §10 Data & Validation Rules, `review_schedule` line: removed the now-moot `ease_factor clamp [1.3, 2.5] (nếu chọn SM-2)` clause and added a note that no `ease_factor` exists since SM-2 was not chosen.
  - Reason for all edits: PRD text contradicted a later, already-approved design decision (DD-04); task VIB-136 required syncing the doc and flagging/justifying any remaining SM-2 terminology.
- `workdir/07-phase1-prd-draft.md/07-phase1-prd-draft.md` — same content, copied to keep this tracked duplicate identical to the canonical draft (both are checked-in copies of the same file, confirmed byte-identical before and after via md5).
- `workdir/phase1-prd-draft.md/07-phase1-prd-draft.md` — same as above.
- `workdir/vib136-comment.md` — temporary file used to post the issue comment via `--content-file`; created and deleted within this session, not part of the final diff.

## New dependencies

None.

## User review

- No code was touched — this was a documentation-only sync task, low risk.
- Three separate tracked copies of the same PRD draft exist in the repo (`attachments/`, `workdir/07-phase1-prd-draft.md/`, `workdir/phase1-prd-draft.md/`); all three were kept in sync. Worth confirming whether having 3 duplicate copies checked into git is intentional — it wasn't in scope for this task to consolidate them.
- §9.2 was marked "RESOLVED" based on DD-04 (VIB-126, approved 2026-08-16) — this reflects an already-approved decision, not a new one made in this session.
- Scope was deliberately kept to the SM-2/BR-REVIEW-002 conflict only; other still-open PRD decisions (§9.3 daily queue cap, §9.4 entry point, etc.) were left untouched even though some were also resolved by later design gates, per the issue's explicit "out of scope" note.
