## Summary

Completed Multica issue VIB-127 (Stage 3 of the Design Team pipeline for P1 Flashcards/SRS/Daily Review): produced information architecture, task/user/screen flows, screen inventory, per-screen state inventory, and exception/recovery paths, grounded directly against the current codebase rather than only the BA PRD. Delivered as a comment + attachment on the Multica issue and self-closed the sub-issue to `done`. No production code was changed.

## Changed files

- `workdir/vib127-stage3-ia-flows.md` — the full Stage 3 deliverable (sitemap, screen inventory of 11 screens/states, Mermaid flows for save-flashcard, flashcard list/flip/unsave/re-save, daily-review entry→session→summary with soft-cap carry-over and crash-resume, both empty states, lesson-delete guard, state inventory table, open-items log, traceability additions). Written to `workdir/` per Multica attachment conventions, then uploaded as an attachment on VIB-127 comment `13a32c57-26db-49f2-bb21-159be18451a1`.
- `workdir/vib127-final-comment.md` — the final summary comment body posted to VIB-127 via `multica issue comment add --content-file`. Deleted after posting (temp file per Comment Formatting convention); no longer present on disk.

No files under `src/` or `docs/` were modified — this was a design/documentation task delivered through the Multica issue system, not a code change.

## New dependencies

None.

## User review

- Two open items were flagged rather than silently resolved, since Gate 1 (VIB-126) didn't explicitly cover them: (1) the Flashcard List (SCR-02) entry point was assumed to be a new header action on `LessonsHistoryScreen` (no tab/nav change) — worth confirming before Stage 4a wireframes it; (2) BR-REVIEW-002's "Again → relearning" wording reads as SM-2-specific language even though Gate 1 resolved the SRS algorithm to fixed-interval (2-outcome rate/skip) — possible terminology mismatch to resolve before Stage 4b writes button copy. Full 6-item open-items table is in §8 of the attached deliverable.
- The lesson-delete guard flow (SCR-09) depends on a real gap confirmed in the codebase: `deleteLesson()` in `src/shared/db/LessonRepository.ts:206` performs a hard delete with no flashcard-awareness today — this needs an engineering precondition check, not just a UI dialog, before this flow can ship safely.
- Issue VIB-127 was moved from `todo` → `in_progress` → `done` as part of this Ownership-mode run; Stage 4 promotion is explicitly left to the Design Lead, per the issue's own instructions.
