## Summary

Completed Stage 3 Requirements Analysis for issue VIB-117 (`Stage 3 — Requirements Analysis: Flashcards, SRS cơ bản, Daily Review (P1 PRD)`), a sub-issue of the BA session VIB-115 (PRD Phase 1 — Flashcards, SRS cơ bản, Daily Review). Work was analysis-only, per the Requirements Analyst agent identity and the issue's explicit constraint not to edit real repo files.

Read required source docs (`02-personas-journey.md`, `04-phase0-prd.md`, `05-phase0-feature-scope.md`, `01-functional-requirements.md`, `02-business-rules.md`, `03-user-stories-acceptance.md`, `01-ai-output-v1.ts`, `05-data-model.md` §6) plus the parent issue's Stage 1 comment (problem statement, scope, glossary, missing-info list, risk register, assumption log), then drafted:

- TO-BE business process (save flashcard → SRS lifecycle → daily review queue → session completion) with a Mermaid diagram and step-by-step FACT/ASSUMPTION labeling.
- New Business Rules (`BR-FLASH-*`, `BR-SRS-*`, `BR-REVIEW-*`).
- Persona-specific JTBD for Minh, An, and Lan (not generic), including a flagged gap where Lan's "reusable sentence pattern" need is not covered by the vocabulary-only core slice.
- New User Stories (Epic 7, `US-016`–`US-020`) with Gherkin acceptance criteria, matching the existing format.
- New Functional Requirements (`FR-FLASH-*`, `FR-SRS-*`, `FR-REVIEW-*`).
- Narrowed answers to Q3 (card scope = vocabulary-only, proposed) and Q4 (session completion = all due cards responded, no accuracy threshold, proposed) — both kept as `QUESTION`, not promoted to `FACT`, per issue instructions.
- Assessment of R3 (SRS ↔ Level-setting dependency): conditionally refuted, with residual risk noted for Technical & Risk Analyst to close depending on the algorithm chosen in Q1.
- Edge cases, UX requirements, consolidated Open Questions / Assumptions / Risks tables, and a traceability section.

The full draft was posted as the root comment on VIB-117 via `multica issue comment add`. Issue status was moved `todo` → `in_progress` → `in_review` per Ownership-mode workflow.

## Changed files

- `stage3-requirements-analysis.md` (repo root) — created temporarily to hold the comment body for `multica issue comment add --content-file` (required convention: agent-authored comments must not use inline `--content`). Deleted immediately after the comment was successfully posted. Net effect: **no persistent file change in the repo.**

No other files in the repository were created, edited, or deleted — consistent with the issue's constraint "Không sửa file thật trong repo — chỉ đăng comment."

## New dependencies

None.

## User review

- The full analysis lives in the VIB-117 comment thread, not in any repo file — review it there.
- Two proposed answers (Q3: vocabulary-only card scope; Q4: session-completion definition) are still labeled `QUESTION` and need explicit confirmation at Human Gate 2, not silently accepted.
- Flagged a new risk (R4): the "reusable sentence pattern" need for persona Lan is not addressed by the vocabulary-only core slice — worth deciding whether to note this as a stated P1 limitation.
- Flagged residual risk (R3) is only conditionally closed — depends on which SRS algorithm the Technical & Risk Analyst (VIB-118) selects for Q1; may need reopening at Stage 4/7 synthesis.
- Two new edge cases (E1: lesson deletion cascade, E2: dedupe-by-VocabularyItem-id vs dedupe-by-word-string) were raised as new open questions not present in the Stage 1 log — need an owner decision at Gate 2.
