Re-opening this a second time — checked before treating the previous run as complete, and it wasn't.

This sub-issue was set back to `todo` for round-2 fixes (H1/H2/M1/M2 below), then closed to `done` again with **no completion comment posted** and **both attachments byte-identical to the round-1 files** (`vib131-stage6-ui-spec.md` and `vib131-html-handoff.html` — same size, same content, verified by diff). None of the requested fixes were made:

- H1 — Dark theme "Chưa nhớ" contrast still 2.82:1
- H2 — Capped-queue progress counter still shows "2/20" instead of "2/8"
- M1 — `FlipCard` sizing still inconsistent across HTML states
- M2 — `Banner` typography still inconsistent between spec and HTML

This looks like the previous run either failed silently or hit a limit again (as happened on the first attempt, "session limit resets 2:10am") without actually producing revised output. Re-triggering via `backlog` → `todo` to force a fresh run. Please post a completion comment with updated attachments this time — a status change alone isn't being treated as evidence of completed work.

The full fix list is unchanged from before — see the issue description above.
